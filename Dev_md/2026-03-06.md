# 개발일지 - 2026-03-06

## DreamIT Biz Docs 프로젝트 초기 구축

### 개요
분야별 학습 자료를 정리하고 브라우저에서 PDF를 바로 열람할 수 있는 문서 라이브러리 사이트를 구축했다.

### 기술 스택
- **프론트엔드**: React 19 + React Router 7
- **빌드 도구**: Vite 6
- **배포**: GitHub Pages (GitHub Actions 자동 배포)
- **도메인**: docs.dreamitbiz.com

### 프로젝트 구조
```
D:\docs\
├── public/pdfs/          # 분야별 PDF 파일 저장소
├── src/config/site.js    # 사이트 설정 (메뉴, 카테고리, 회사정보)
├── src/data/documents.js # 문서 목록 데이터
├── src/pages/            # 홈, 카테고리, 뷰어, 404 페이지
├── src/components/       # Navbar, Footer, CategoryCard, PDFViewer 등
├── src/contexts/         # ThemeContext, ToastContext
├── src/styles/           # CSS 스타일시트 (12개 파일)
└── .github/workflows/    # CI/CD 자동 배포
```

### 구현 내용

#### 1. 프로젝트 기본 셋업
- React + Vite 프로젝트 초기화
- GitHub Actions를 통한 GitHub Pages 자동 배포 워크플로우 설정
- CNAME 파일 추가 (docs.dreamitbiz.com 커스텀 도메인)

#### 2. 카테고리 시스템 (8개 분야)
| 분야 | ID | 설명 |
|------|-----|------|
| 프로그래밍 | programming | Python, JavaScript, Java 등 |
| 컴퓨터과학 | computer-science | 자료구조, 알고리즘, OS, 네트워크 |
| 수학 | mathematics | 미적분, 선형대수, 통계 |
| 과학 | science | 물리, 화학, 생물 |
| 영어 | english | 문법, 독해, TOEIC |
| 경제/경영 | business | 경제학, 경영학 |
| 자격증 | certifications | 정보처리기사, AWS 등 |
| 기타 | others | 기타 학습 자료 |

#### 3. 주요 기능
- **PDF 바로보기**: iframe 기반 브라우저 내장 PDF 뷰어
- **문서 검색**: Ctrl+K 단축키, 제목/설명/태그 검색
- **테마 시스템**: Dark/Light/Auto 모드 + 5가지 색상 테마
- **반응형 디자인**: 데스크톱, 태블릿, 모바일 대응
- **AOS 애니메이션**: 스크롤 시 요소 등장 효과
- **Glassmorphism 네비바**: blur 효과 적용 고정 네비게이션

#### 4. 브랜딩 및 푸터
- 로고: **DreamIT Biz Docs** (각 단어별 색상 차별화)
- 푸터: www.dreamitbiz.com과 동일한 구조
  - 회사 정보 (사업자등록번호, 통신판매신고번호 등)
  - 연락처 (주소, 이메일, 전화, 카카오톡)
  - Family Site 드롭다운 (본사이트, Books, AHP, 핵심역량)
  - Copyright: 2020-현재

---

## 관리자 CRUD + 회원 관리 시스템 추가

### 개요
정적 데이터(documents.js)만 사용하던 사이트에 Supabase 기반 관리자 CRUD 게시판과 회원 관리 기능을 추가했다. Supabase 미설정 시 기존 정적 데이터로 정상 동작하는 graceful fallback 구조.

### 추가 기술 스택
- **백엔드/DB**: Supabase (PostgreSQL + Auth + Storage)
- **패키지 추가**: `@supabase/supabase-js`

### 구현 단계

#### Phase 1: Foundation (기반 구축)
- **`src/utils/supabase.js`** - Supabase 클라이언트 (env 없으면 null 반환)
- **`src/config/admin.js`** - 관리자 이메일 화이트리스트 (`aebon@dreamitbiz.com`)
- **`src/services/documents.js`** - 데이터 추상화 레이어
  - Supabase 연결 시 DB에서 조회, 미연결 시 정적 데이터 fallback
  - CRUD 함수: `fetchDocuments`, `createDocument`, `updateDocument`, `deleteDocument`, `deleteDocuments`, `seedDocuments`
  - 검색/집계: `searchDocumentsAsync`, `fetchCategoryCounts`
- **`.env.example`** - 환경변수 템플릿

#### Phase 2: Auth (인증 시스템)
- **`src/contexts/AuthContext.jsx`** - 인증 상태 관리
  - `signInWithEmail`, `signOut`, `isAdminUser` 제공
  - Supabase Auth 세션 리스닝
- **`src/components/auth/LoginModal.jsx`** - 이메일/비밀번호 로그인 모달
- **`src/components/auth/AuthGuard.jsx`** - 관리자 전용 라우트 보호 (비관리자 → 홈 리다이렉트)
- **`src/styles/auth.css`** - 모달/폼 스타일 (다크모드 대응)

#### Phase 3: Admin Shell (관리자 셸)
- **`src/layouts/AdminLayout.jsx`** - 사이드바 + Outlet 레이아웃
  - 네비게이션: 대시보드, 문서 관리, 회원 관리
  - 사용자 정보 표시, 로그아웃 버튼
  - 모바일 반응형 드로어
- **`src/pages/admin/AdminDashboard.jsx`** - 대시보드
  - 통계 카드 (전체 문서, 활성 카테고리, 전체 카테고리)
  - 최근 문서 테이블
  - 카테고리별 현황 막대 차트
- **`src/styles/admin.css`** - 관리자 UI 스타일 시스템 (레이아웃, 테이블, 폼, 버튼, 배지, 툴바)

#### Phase 4: Document CRUD (문서 관리)
- **`src/utils/storage.js`** - Supabase Storage PDF 업로드/삭제
- **`src/pages/admin/AdminDocuments.jsx`** - 문서 목록 페이지
  - 검색, 체크박스 선택, 벌크 삭제
  - 더미 데이터 시딩 버튼
  - 행별 수정/삭제
- **`src/pages/admin/AdminDocumentForm.jsx`** - 문서 생성/수정 폼
  - 슬러그, 카테고리, 제목, 설명, PDF 업로드, 페이지 수, 태그, 추가일

#### Phase 5: Member Management (회원 관리)
- **`src/services/members.js`** - 프로필 조회 (`fetchMembers`, `fetchMemberCount`)
- **`src/pages/admin/AdminMembers.jsx`** - 회원 목록 페이지
  - 아바타, 이름, 이메일, 관리자 배지, 가입일
  - 이름/이메일 검색

#### Phase 6: Public Page Migration (공개 페이지 전환)
- **Home, Category, Viewer** → `useState`/`useEffect` 비동기 데이터 로딩으로 전환
- **Navbar** → `searchDocumentsAsync` 비동기 검색 + 로그인/관리자 버튼 추가
- **CategoryCard** → `docCount` prop으로 카운트 외부 주입

### 변경된 파일 구조
```
src/
├── utils/
│   ├── supabase.js          # [신규] Supabase 클라이언트
│   └── storage.js           # [신규] PDF 업로드/삭제
├── config/
│   ├── site.js
│   └── admin.js             # [신규] 관리자 이메일 설정
├── services/
│   ├── documents.js         # [신규] 문서 데이터 서비스
│   └── members.js           # [신규] 회원 데이터 서비스
├── contexts/
│   ├── ThemeContext.jsx
│   ├── ToastContext.jsx
│   └── AuthContext.jsx       # [신규] 인증 컨텍스트
├── components/
│   ├── auth/
│   │   ├── LoginModal.jsx    # [신규] 로그인 모달
│   │   └── AuthGuard.jsx     # [신규] 라우트 보호
│   └── layout/
│       ├── Navbar.jsx        # [수정] 비동기 검색 + 로그인 버튼
│       └── Footer.jsx
├── layouts/
│   ├── PublicLayout.jsx
│   └── AdminLayout.jsx      # [신규] 관리자 레이아웃
├── pages/
│   ├── Home.jsx              # [수정] 비동기 로딩
│   ├── Category.jsx          # [수정] 비동기 로딩
│   ├── Viewer.jsx            # [수정] 비동기 로딩
│   ├── NotFound.jsx
│   └── admin/
│       ├── AdminDashboard.jsx     # [신규]
│       ├── AdminDocuments.jsx     # [신규]
│       ├── AdminDocumentForm.jsx  # [신규]
│       └── AdminMembers.jsx       # [신규]
├── styles/
│   ├── auth.css              # [신규]
│   └── admin.css             # [신규]
└── App.jsx                   # [수정] AuthProvider + 관리자 라우트
```

### Supabase 테이블 스키마
```sql
-- documents 테이블
CREATE TABLE documents (
  id          BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  slug        TEXT UNIQUE NOT NULL,
  category    TEXT NOT NULL,
  title       TEXT NOT NULL,
  description TEXT DEFAULT '',
  file_name   TEXT DEFAULT '',
  file_url    TEXT DEFAULT '',
  file_size   TEXT DEFAULT '',
  pages       INT,
  tags        TEXT[] DEFAULT '{}',
  added_at    DATE DEFAULT CURRENT_DATE
);

-- profiles 테이블 (auth trigger로 자동 생성)
CREATE TABLE profiles (
  id          UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email       TEXT,
  full_name   TEXT,
  avatar_url  TEXT,
  created_at  TIMESTAMPTZ DEFAULT NOW()
);

-- auth trigger
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email)
  VALUES (NEW.id, NEW.email);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
AFTER INSERT ON auth.users
FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

### 라우트 구조
| 경로 | 컴포넌트 | 접근 권한 |
|------|----------|----------|
| `/` | Home | 공개 |
| `/category/:id` | Category | 공개 |
| `/view/:docId` | Viewer | 공개 |
| `/admin` | AdminDashboard | 관리자 |
| `/admin/documents` | AdminDocuments | 관리자 |
| `/admin/documents/new` | AdminDocumentForm | 관리자 |
| `/admin/documents/:id/edit` | AdminDocumentForm | 관리자 |
| `/admin/members` | AdminMembers | 관리자 |

### 커밋 이력
1. `790f4b5` - Initial commit: 프로젝트 전체 구조 생성 (47파일)
2. `c5158c0` - CNAME 추가 및 SPA 404 fallback 설정
3. `155e43e` - DreamIT Biz Docs 브랜딩 및 템플릿 푸터 적용
4. `26156d7` - 로고 업데이트 및 푸터 카테고리 링크 추가
5. `3868b8d` - 히어로/카테고리 헤더/뷰어 헤더에 다크 블루 그라데이션 적용

### 다음 작업
- [ ] Supabase 프로젝트 생성 및 테이블/트리거 배포
- [ ] Supabase Storage 버킷(documents) 생성
- [ ] 환경변수 설정 (.env)
- [ ] 관리자 계정 생성 (aebon@dreamitbiz.com)
- [ ] 정적 데이터 → Supabase DB 시딩
- [ ] 실제 PDF 자료 업로드
